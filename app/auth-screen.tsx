'use client';
import {useState,FormEvent} from 'react';
import {Leaf,LockKeyhole} from 'lucide-react';
export default function AuthScreen(){const [signup,setSignup]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setError('');try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:signup?'signup':'login',username:f.get('username'),password:f.get('password')})});const d:any=await r.json();if(!r.ok)throw Error(d.error);location.replace('/diary')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
return <main className="auth-shell"><section className="auth-card"><div className="brand"><span className="brand-icon"><Leaf/></span><div>큐티스트릿도 <strong>다이어리</strong><small>MY LITTLE ISLAND JOURNAL</small></div></div><div className="auth-title"><LockKeyhole size={28}/><h1>{signup?'나만의 다이어리 만들기':'다시 만나서 반가워요'}</h1><p>섬에서 보낸 하루, 내 계정 안에 소중히.</p></div><form onSubmit={submit}><label>아이디<input name="username" autoComplete="username" pattern="[a-z0-9_]{4,32}" minLength={4} maxLength={32} required placeholder="영문 소문자·숫자·밑줄 4~32자"/></label><label>비밀번호<input name="password" type="password" autoComplete={signup?'new-password':'current-password'} minLength={12} required placeholder="12자 이상 · 최대 72바이트"/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="primary" disabled={busy}>{busy?'확인하고 있어요…':signup?'가입하고 시작하기':'로그인'}</button></form><button className="auth-switch" onClick={()=>{setSignup(!signup);setError('')}}>{signup?'이미 계정이 있어요 · 로그인':'처음 왔어요 · 회원가입'}</button><p className="auth-note">내 계획과 플레이 기록은 로그인한 나에게만 보여요.</p></section><figure className="auth-image"><img src="/photos/street.jpg" alt="큐티스트릿도 상점가 풍경"/><figcaption>작은 계획이 모여, 우리 섬의 다음 풍경.</figcaption></figure></main>}
