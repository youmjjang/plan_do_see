'use client';
import {useState,FormEvent} from 'react';
import Observation from './observation';
export default function AccountPanel(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);
async function action(action:string,currentPassword?:string,newPassword?:string){setBusy(true);setError('');try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,currentPassword,newPassword})});const d:any=await r.json();if(!r.ok)throw Error(d.error);location.replace('/')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);void action(f.get('action') as string,f.get('currentPassword') as string,f.get('newPassword') as string)}
return <><div className="account-bar"><details><summary>내 계정 · 보안 설정</summary><div className="account-content"><p>12시간 뒤 로그인이 만료돼요. 로그아웃·비밀번호 변경 시 모든 기기의 기존 로그인이 종료돼요.</p><a href="/claim">T06 기록을 내 계정으로 가져오기</a><form onSubmit={submit}><h3>비밀번호 변경</h3><input type="hidden" name="action" value="password"/><label>현재 비밀번호<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>새 비밀번호<input name="newPassword" type="password" autoComplete="new-password" required minLength={12}/></label><button disabled={busy}>변경하고 로그아웃</button></form><form onSubmit={submit}><h3>계정 삭제</h3><p>내 계획·실행 기록·관찰 기록이 함께 영구 삭제돼요. 먼저 내 자료를 내보내 주세요.</p><input type="hidden" name="action" value="delete"/><label>현재 비밀번호<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label><input type="checkbox" required/> 내 계정과 기록을 영구 삭제하는 데 동의해요.</label><button className="danger" disabled={busy}>계정과 내 기록 삭제</button></form>{error&&<p className="error" role="alert">{error}</p>}</div></details><button disabled={busy} onClick={()=>action('logout')}>로그아웃</button></div><Observation/></>}
