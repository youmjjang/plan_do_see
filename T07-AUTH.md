# T07 · 큐티스트릿도 다이어리 인증 구현 설명서

이번 범위: 기존 앱과 인증 기능의 보완·검증·문서 정리 및 사용자가 제공한 2026-09-15~09-19 실제 관찰 내보내기의 검토. 합성 테스트와 실제 관찰을 구분하며, 사용자의 직접 손계산·화면 대조는 확인 전까지 완료로 표시하지 않는다.

## ① 무엇으로 붙였나

- 기존 T06 결과: https://cutie-street-island-diary.youmnana19.chatgpt.site/
- 최종 T06 고정 소스: https://github.com/youmjjang/plan_do_see/tree/8cb57e4f9f70ec98e76f4b43164499dc69b9cacc
- 이 T07은 위 Git 이력과 기존 앱·D1 데이터베이스를 이어서 수정했다.
- 인증 방식: **앱 자체 세션 인증을 직접 구현**했다. 완성형 인증 프레임워크를 사용했다고 주장하지 않는다. 비밀번호 해시·검증은 **bcryptjs 3.0.3**, **bcrypt 비용 계수 12**에 맡겼다. salt는 라이브러리가 계정마다 생성한다.
- 서버: 기존 Vinext 1.0.0-beta.5, Cloudflare Workers, D1. 라이브러리의 정확한 의존성은 package-lock.json에 고정했다.
- 브라우저에는 256비트 난수 세션을 HttpOnly·SameSite=Strict 쿠키로 전달하고 HTTPS에서 Secure를 붙인다. D1에는 세션 원문 대신 SHA-256 지문만 보관한다.
- 세션은 발급 뒤 **12시간**에 만료된다. 로그아웃·비밀번호 변경 시 모든 기존 세션을 서버에서 삭제한다. 동시에 진행되던 로그인도 세션 세대값을 검사하여 오래된 인증 상태로 세션을 만들지 못하게 한다.
- 기존 T06 자료는 owner_id가 없는 상태에서 일반 회원에게 보이지 않는다. 로그인한 다이어리 계정과 기존 소유자의 ChatGPT 소유자 확인을 함께 거쳐 연결한다. **C100: 2026-09-16에 소유자 확인을 거쳐 기존 계획 5개를 현재 다이어리 계정으로 연결했다.** 연결 완료 화면과 계정 내 ‘유럽·모던풍 상점가 꾸미기’, ‘물고기 도감 채우기’, 다음 계획 및 기존 할 일 목록으로 확인했다. 날짜별 데일리 계획은 기존 통합 기능에 따라 하나로 모일 수 있다.

## ② 왜 그걸 골랐나

- 기존 Workers/D1 구조를 유지하고, 과제에서 요구하는 비밀번호 저장값과 로그아웃 후 같은 세션 거절을 직접 확인하기 위해 선택했다.
- bcryptjs는 Workers에서도 사용할 수 있는 JavaScript 라이브러리이고, salt 생성과 비밀번호 검증을 검증된 구현에 맡길 수 있다. 입력은 최소 12자, 최대 UTF-8 72바이트로 제한해 bcrypt의 입력 잘림을 방지한다.
- 검토한 다른 방법: ChatGPT 로그인만으로 앱 전체를 잠그는 방법. 편리하지만 앱 자체의 가입·비밀번호 저장값 비교를 보여 주어야 하는 과제와 맞지 않아 주 인증으로 쓰지 않았다. 기존 T06 소유권 확인에만 사용한다.
- 검토한 다른 방법: 자체 서명 JWT. 만료 전 로그아웃을 즉시 반영하려면 별도 폐기 저장소가 필요하므로, 이번에는 D1 서버 세션이 더 단순했다.
- 참고: https://www.npmjs.com/package/bcryptjs 및 https://developers.cloudflare.com/workers/runtime-apis/web-crypto/

## ③ 어디를 어떻게 고쳤나

| 흐름 | 소스 위치와 동작 |
|---|---|
| 가입 | app/auth-screen.tsx → app/api/auth/route.ts POST signup → lib/auth.ts hashPassword → auth_users의 고유 username 및 비밀번호 해시 저장 → newSession |
| 로그인 | app/auth-screen.tsx → app/api/auth/route.ts POST login → bcrypt 비교 → D1 세션 발급. 아이디 없음과 비밀번호 틀림은 같은 401 문구 |
| 로그아웃 | app/account-panel.tsx → app/api/auth/route.ts POST logout → 세션 세대 증가와 해당 회원의 세션 전체 삭제 → 쿠키 만료 |
| 자료 조회 | app/diary/page.tsx에서 세션 확인 → app/api/diary/route.ts GET에서 다시 requireUser → snapshot이 plans.owner_id 및 자식 테이블 JOIN으로 범위 제한 |
| 자료 변경 | app/api/diary/route.ts POST에서 세션과 Origin 확인 → owned가 계획·할 일·실행의 주인을 검사 → 통과한 요청만 저장. 새 사용자 입력 ID는 UUID만 허용해 내부 루틴 ID 충돌 방지 |
| 관찰 기록 | app/observation.tsx → app/api/observation/route.ts. 서버가 서울 날짜와 저장 시각을 정하고 회원·날짜 고유 제약을 적용 |
| 내보내기 | app/api/export/route.ts. 로그인 회원의 다이어리와 관찰 자료를 JSON 하나로 반환. 비밀번호·세션 테이블 제외 |
| 계정 삭제 | app/api/auth/route.ts POST delete. 현재 비밀번호 재확인 뒤 자식 자료·관찰·세션·계정을 한 D1 batch로 삭제 |
| T06 이관 | app/claim/page.tsx와 app/api/claim/route.ts. 플랫폼이 확인한 기존 소유자와 현재 다이어리 로그인 모두 필요 |

소유자가 보낸 URL·헤더·본문의 user_id/owner_id는 권한 판단에 쓰지 않는다. 성공·실패 응답은 비밀번호나 세션 원문을 JSON으로 반환하지 않는다. 앱 코드에는 요청 본문을 출력하는 로그가 없다.

## ④ 안 열리는 것을 확인한 기록

`evidence/t07/http-evidence.json`은 테스트 계정 A/B의 **실제 HTTP 요청·응답**이다. 비밀번호와 쿠키를 파일 작성 전에 가렸다. `evidence/t07/password-hashes.json`은 로컬 테스트 DB에서 읽은 서로 다른 두 bcrypt 저장값이다. 실제 사용자의 비밀번호 해시가 아니다.

확인 다섯 가지는 아래 성공·거절 쌍으로 묶었다. 각 쌍의 요청 주소·방식·본문·응답은 첨부 기록의 label로 찾을 수 있다.

1. **비로그인 접근**: 로그인한 계정의 `GET /api/diary` 200 ↔ 쿠키 없는 같은 요청 401. `GET /`은 누구나 200, `GET /diary`는 로그인 없이 307 이동.
2. **로그인 신원 확인**: 가입한 계정의 올바른 로그인 200 ↔ 잘못된 비밀번호/없는 아이디 모두 401이며 안내 문구 일치. 중복 가입은 409.
3. **세션 폐기**: 로그아웃 직전 `GET /api/diary` 200 ↔ 로그아웃 후 **같은 쿠키·주소·GET** 401. 비밀번호 변경 전후에도 이전 쿠키의 200/401을 비교했다. 별도 로컬 검사에서 세션 만료 후 401을 확인했다.
4. **다른 회원 자료**: A의 자기 자료 읽기·수정·삭제 성공 ↔ A→B 읽기·수정·삭제 각 404. B→A도 같은 세 가지가 404. 공격 전후 전체 응답을 비교해 건수·내용·삭제 표시가 바뀌지 않았고 새 자료도 생기지 않았음을 확인했다.
5. **소유자 위조·목록 분리**: 정상 자기 목록 200 ↔ 다른 회원을 URL·헤더·본문에 넣어도 자기 목록만 반환. 다른 회원 계획에 새 할 일·실행을 붙이려는 요청은 404. 다른 Origin에서 쓰기는 403.

합격을 미리 가정하지 않고 검사 스크립트가 응답 코드를 assert한다. `tests/verify-t07.mjs`가 임시 로컬 D1과 빌드된 Worker를 사용해 `tests/auth.mjs`, `tests/observation.mjs`, `tests/stats.mjs`를 순서대로 실행한다. 이번 검증은 인증 HTTP 60개, 관찰·만료 HTTP 23개와 집계 검사를 통과했다. 실제 요청 시각과 응답은 증거 파일에 남겼다. 관찰 검사에서 사용하는 날짜는 **로컬 테스트 데이터**이며 실제 5일 관찰의 증빙이 아니다. 검사를 마치면 테스트 계정을 삭제한다.

## ⑤ AI와 나

- AI에게 맡긴 일: 기존 T06 코드 확인, 인증·접근 제한·관찰 기록 구현, 테스트 요청 실행, 증거 파일과 설명서 초안 정리, 사용자가 제공한 실제 5일 내보내기 검토 및 합계·평균 재계산.
- 내가 직접 판단한 일: T06의 동숲 다이어리를 이어서 쓰며, 이번 과제의 다섯 카드 조건에 맞춰 구현하도록 요청했다. 실제 관찰 질문·계획 규칙·매일 활동과 변경 이유는 내가 확인하고 기록한다.
- AI 결과를 그대로 받지 않은 일: 사용자는 쭈니가 잘린 사진 표시를 반복해서 지적하고, 주민이 보이도록 위치를 조정하거나 사진을 바꾸도록 요청했다. 또한 처음 구현을 요청할 때 실제 5일 기록의 대리 작성을 제외했고, 이후 실제 관찰 기록을 직접 입력했다.

위 세 줄은 대화에서 확인된 사실에 근거한 초안이다. 사용자가 테스트를 직접 수행했다고 표현하지 않는다.

## ⑥ 아직 못 막은 것

- 비밀번호 분실 복구와 다중 인증은 구현하지 않았다. 비밀번호를 잊으면 스스로 복구할 수 없고, 비밀번호를 빼앗기면 추가 인증 없이 로그인할 수 있다.
- 아이디·IP별 15분 요청 제한은 있지만 분산 공격·봇 가입·장기간 공격 탐지·알림까지 제공하지 않는다. 많은 출발지의 공격으로 서비스가 느려지거나 계정별 제한이 악용될 수 있다.
- 소유자 연결은 Sites가 인증 헤더를 신뢰할 수 있게 전달한다는 플랫폼 경계에 의존한다. 독립 서버로 이전할 때는 외부가 이 헤더를 직접 위조하지 못하도록 새 검증이 필요하다.
- 과거 T06이 공개였던 기간에 타인이 내려받은 자료나 과거 공개 Git 소스는 소급해서 회수할 수 없다. 인증은 새 앱의 저장된 비공개 기록에 적용된다.
- 모든 미래 공격을 막았다고 주장하지 않는다. 테스트로 확인한 범위와 아직 확인하지 않은 범위를 구분한다.

## 이번 보완 내용

- 가입 후 모든 사용자를 T06 소유자 확인으로 보내던 흐름을 바꿨다. 이제 자신의 빈 다이어리로 이동하며 기존 기록 연결은 계정 설정에서 선택한다.
- 로그인 사진은 가로 비율을 유지해서 전체가 보이도록 표시한다. 세로 틀에 맞추면서 주민이 잘리는 것을 없앴다.
- JSON 요청이 null·배열·기본값이면 400으로 거절한다.
- 같은 날 관찰을 동시에 저장할 때 한 건만 남기고 다른 요청은 409로 알린다. 관찰 시작과 규칙 변경도 이미 저장됐으면 성공으로 가장하지 않는다.
- 관찰 0일에는 평균을 0으로 만들지 않으며 5일 미만은 중간 집계로 표시한다.
- README와 제출 안내를 현재 T07에 맞췄고, 검증 시 임시 DB를 제거한다.

섬 이름·개인 사진 업로드는 인증 과제 마무리의 필수 기능으로 추가하지 않았다. 기존 테마 사진은 유지했다.

## 실제 5일 관찰 결과 · 2026-09-19 확인

사용자가 제공한 전체 내보내기 파일 `큐티스트릿도-전체기록.json`(schema: pds-t07-v1)을 AI가 읽고 재계산했다. 내보내기 시각은 2026-09-19 01:35:14 KST이다. 아래는 실제 계정의 관찰 기록 요약이며 합성 테스트와 구분한다. 전체 내보내기 원본은 별도 제출 자료로 보관한다.

- 질문: 내가 선물을 주다가 쉬면 뽀야미가 나에게 선물을 줄까?
- 지표·단위: 뽀야미에게 선물 받음 여부, 0/1.
- 계산 규칙: 답례를 포함해 하루 한 번 이상 받으면 1, 받지 못하면 0. 여러 번 받아도 1이다. 합계는 받은 날 수, 평균은 받은 날의 비율이다.
- 미관찰일은 제외하며, 만났으나 받지 못한 날은 0이다. 날짜별 한 건만 기록하고 0·1 이외의 값은 실제 결과에 맞게 정정한다. 평균은 소수 셋째 자리에서 반올림하여 둘째 자리까지 표시하고, 주 시작은 화요일이다.

| 일차 | 날짜(KST) | 값 | 관찰 메모 요약 |
|---|---|---:|---|
| 1 | 2026-09-15 | 1 | 옷을 주고 과일을 받음 |
| 2 | 2026-09-16 | 1 | 배 스무디를 주고 야구재킷을 받음 |
| 3 | 2026-09-17 | 0 | 대화만 했고 선물을 받지 못함 |
| 4 | 2026-09-18 | 1 | 뽀야미가 먼저 와서 옷을 줌 |
| 5 | 2026-09-19 | 0 | 대화를 걸었으나 받지 못함 |

다섯 기록은 같은 계정에 속하며 날짜가 중복되지 않는다. 각 생성 시각을 KST로 바꾸면 해당 관찰 날짜와 일치한다.

### 계획 규칙 변경과 순서

- 기존: 뽀야미에게 하루 한 번 선물하기.
- 변경: 3~5일차에는 선물을 주지 않고 하루 한 번 대화만 하기.
- 이유: 첫 이틀에는 선물을 주고 답례를 받았으므로, 주지 않고 대화만 해도 선물을 받는지 확인한다.
- 2일차 저장: 2026-09-16 08:54:52 KST.
- 규칙 변경: 2026-09-16 09:04:22 KST.
- 3일차 저장: 2026-09-17 08:56:36 KST.
- 변경 기록의 based_on은 실제 1·2일차 기록의 ID·날짜·값과 일치한다. 따라서 2일차 저장 뒤, 3일차 저장 전이라는 순서를 충족한다.

### 지표 정정 이력

1일차 14:25:03 KST에 앱의 기본 지표 ‘완료한 계획 항목 수/개’를 실제 관찰 의도에 맞는 ‘선물 받음 여부/0·1’로 정정했다. 정정은 1일차 최초 기록(14:19:43 KST) 뒤에 이루어졌으며, 기존 날짜·값·선물 관련 메모는 유지됐다. 이 이력을 숨기거나 최초 저장 때부터 지표 설정이 정확했다고 주장하지 않는다. 현재 5일 집계는 모두 동일한 선물 여부 기준으로 계산했다.

### 계산 대조와 남은 본인 확인(C132)

| 항목 | 기록에서 재계산 | 내보내기 집계 | 대조 |
|---|---|---|---|
| 합계 | 1+1+0+1+0 = 3 | sum = 3 | 일치 |
| 전체 평균 | 3÷5 = 0.60 | average = 0.6 | 일치 |
| 변경 전 평균 | (1+1)÷2 = 1.00 | before = 1 | 일치 |
| 변경 후 평균 | (0+1+0)÷3 = 0.333… → 0.33 | after = 0.33 | 일치 |

선물을 주지 않은 3일 중 하루에도 선물을 받았다. 다만 짧은 5일 관찰만으로 규칙 변경이 결과의 원인이라고 단정하지 않는다.

**확인 범위:** AI가 원본 JSON과 계산을 대조했다. 앱 화면을 직접 대조하거나 사용자가 손계산을 마쳤다는 증거는 이번 확인에 포함되지 않는다. C132의 본인 확인은 사용자가 위 계산을 직접 수행하고 앱 화면의 합계 3·평균 0.60과 대조한 후 완료로 표시한다.
